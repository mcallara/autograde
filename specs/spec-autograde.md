Create a public repository named `autograde` on GitHub. The Python CLI tool needs to take as input a valid repository URL and return if the following conditions are met:

1. The repository should be a git repository.
2. The repository should have a `main` branch.
3. The repository should have a `feature` branch in the remote.
4. The `file1.txt`should exist in the `main` branch.

