# AGENTS.md

Project conventions for all coding agents working in this repository:

1. Use `uv` for Python dependency management.
2. Use `pytest` for running tests.
3. Keep all tests in a top-level `tests/` folder at the repository root.
